<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kult">

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StaticVideoActivity" />

        <activity
            android:name=".VideoIdUsingCatalogActivity"
            android:configChanges="orientation|screenSize" />

        <activity android:name=".KultVideoListActivity" />
        <activity android:name=".KultProductActivity_NYX" />
        <activity android:name=".ExoPlayerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|layoutDirection"/>
        <activity android:name=".KultVideoListExoPlayerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|layoutDirection"/>
    </application>

</manifest>