<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kult">

    <application
        android:name=".exoplayer.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".brightcove.BrightCove_StaticVideoActivity" />

        <activity
            android:name=".brightcove.BrightCove_VideoActivity"
            android:configChanges="orientation|screenSize" />

        <activity android:name=".brightcove.BrightCove_VideoListActivity" />

        <activity android:name=".brightcove.BrightCove_AnalyticsActivity" />

        <activity android:name=".brightcove.BrightCove_NYX" />

        <activity
            android:name=".exoplayer.ExoPlayer_VideoActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|layoutDirection" />

        <activity
            android:name=".exoplayer.ExoPlayer_VideoListActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|layoutDirection" />

    </application>

</manifest>